# 🐇 Pyoco

**Pythonのための、最高にかわいいワークフローエンジン。** ✨

Pyoco は、データパイプラインや自動化タスクを「機能的」なだけでなく「楽しい」ものにするために設計された、軽量で開発者に優しいワークフローエンジンです。

シンプルさと開発者体験（DX）に焦点を当て、Pyoco はあなたの毎日のコーディングに魔法の粉を振りかけます。

## ✨ 特徴

- **🍰 シンプルな DSL**: `@task` デコレータでタスクを定義し、`>>` でつなぐだけ。
- **🐇 かわいい出力**: 絵文字たっぷりの美しいコンソールログで、デバッグも楽しくなります。
- **🚀 並列実行**: 独立したタスクを自動的に並列実行します。
- **🛡️ 堅牢**: リトライ、タイムアウト、失敗時のポリシー設定などを標準サポート。
- **📦 アーティファクト管理**: タスクの出力やファイルを簡単に保存・管理できます。

## 📦 インストール

```bash
pip install pyoco
```

## 🚀 クイックスタート

`tasks.py` を作成します:

```python
from pyoco import task

@task
def hello(ctx):
    print("Hello, Pyoco! 🐇")
    return "World"

@task
def greet(ctx, name):
    print(f"Nice to meet you, {name}! ✨")
```

`flow.yaml` を作成します:

```yaml
version: 1
discovery:
  glob_modules: ["tasks.py"]

tasks:
  greet:
    inputs:
      name: "$node.hello.output"

flows:
  main:
    graph: |
      hello >> greet
```

実行してみましょう！

```bash
pyoco run --config flow.yaml
```

こんな風に表示されます:

```
🐇 pyoco > start flow=main
🏃 start node=hello
Hello, Pyoco! 🐇
✅ done node=hello (0.12 ms)
🏃 start node=greet
Nice to meet you, World! ✨
✅ done node=greet (0.08 ms)
🥕 done flow=main
```

## 📚 ドキュメント

- [チュートリアル](docs/tutorial/index.md)
- [ロードマップ](docs/roadmap.md)

## 💖 コントリビューション

プルリクエストをお待ちしています！

---

*Made with 🥕 by the Pyoco Team.*
