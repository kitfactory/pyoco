# v0.2.0 Implementation Tasks

## 1. Core State Models
- [x] Define `TaskState` & `RunStatus` enums
- [x] Create `RunContext` class
- [x] Integrate `RunContext` into `Engine` (via Context)

## 2. Engine State Management
- [x] Generate `run_id` in `Engine.run`
- [x] Track `TaskState` transitions in `Engine`

## 3. Cancellation Logic
- [x] Implement `Engine.cancel(run_id)`
- [x] Handle cancellation in execution loop
- [x] Add `ctx.is_cancelled` for cooperative cancellation

## 4. CLI & Verification
- [x] Display Run ID in trace output
- [x] Handle `Ctrl+C` to trigger cancellation
- [x] Verify state transitions and cancellation behavior
