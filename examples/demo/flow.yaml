version: 1
flows:
  main:
    graph: |
      A >> (B & C)
    defaults:
      x: 10

discovery:
  packages: []
  glob_modules: ["tasks"] # Load tasks.py from current dir

tasks:
  A:
    callable: tasks:A
  B:
    callable: tasks:B
  C:
    callable: tasks:C
